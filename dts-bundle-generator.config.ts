const config = {
    entries: [
        {
            filePath: "./src/editor.ts",
            outFile: "./build/dist/index.d.ts",
            noCheck: false,
        },
    ],
}

module.exports = config;